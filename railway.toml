[build]
buildCommand = "pip install -r requirements.txt && PLAYWRIGHT_BROWSERS_PATH=/app/.playwright playwright install chromium --with-deps"

[deploy]
startCommand = "PLAYWRIGHT_BROWSERS_PATH=/app/.playwright python soho_appt_monitor.py"
